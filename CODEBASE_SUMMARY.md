# Codebase Summary

このワークスペースは **Visual Studio Code (VS Code) のフォーク／worktree** です。
`.claude/worktrees/funny-aryabhata/` 配下に VS Code のソースツリーがチェックアウトされており、
ブランチ単位で並行作業するための git worktree として運用されています。

## 1. リポジトリのルート構成（推定）

